<div class="container">

<nav aria-label="breadcrumb ">
  <ol class="breadcrumb background-transparent">
    <li class="breadcrumb-item"><%= link_to "Menu", menu_path %></li>
    <li class="breadcrumb-item active" aria-current="page"><%= params[:category].capitalize %></li>
  </ol>
</nav>
<h1><%= params[:category].capitalize %></h1>
<hr>

 <% @drinks.each do |drink| %>
    <div class="flex-box">
      <div class="flex-item flex-item-1 my-5">
        <%= link_to drink_path(drink) do %>
      <%= image_tag "#{drink.name.downcase}", alt: drink.name, class: 'rounded-img' %>
      <% end %>
     </div>
     <div class="flex-item flex-item-2">
        <%= link_to drink_path(drink) do  %>
       <h4><strong><%= drink.name %></strong></h4>
       <% end %>
       <p><%= drink.description%></p>
       <hr>
       <p>£<%= drink.price %></p>
    </div>
  </div>
   <% end %>


</div>
